Esse padrão é um utilizado durante o design da arquitetura e tem como intenção fazer a separação entre abstração ( código de alto nível ) e implementação ( código de baixo nível ).
O código dele funciona de maneira extremamente similar a do [[Adapter]].
